# Cadence and Template Editor Tasks

## Tasks

T1: Install shadcn `tabs` primitive; replace IconPlaceholder if present.
T2: Build `CadenceTemplatesView` composing PageHeader + Tabs + sub-views.
T3: Replace `cadences.tsx` route content (add validateSearch, render the view).
T4: Delete `CadencesManager` (unused).
T5: bun check.
T6: Chrome validation (if dev server is up).
T7: PR + CI + squash.

All thin → no tests.
